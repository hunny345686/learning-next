import "server-only"
export const serverSideFunc=()=>{
    console.log(`
        -Use multiple library
        - env variable
        -db querys
        - confidential opes 
        
        `)

        return "Server Result"
}